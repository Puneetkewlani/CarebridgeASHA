rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow all reads/writes temporarily for testing
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
